import * as Types from "../../../src/types";

export const mockServicePropertiesResponse: Types.ServiceProperties = {
    DI_HOST: "https://example.com",
    DI_SUBSCRIPTION_KEY: "test-subscription-key",
};
